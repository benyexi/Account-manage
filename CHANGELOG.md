# 更新日志

## 2026-07-23

### 新增

- 将用户入口页面改为 `public/锁服.html`，根路径 `/` 自动加载该页面。
- 登录后展示未授权或已授权状态，支持管理员设置 1 天、1 个月和永久授权。
- 新增积分与卡槽商城，300 积分可购买 1 个任务卡槽，最多 10 个。
- 新增安全的 TCP 端口联通性检查任务，支持 IP、端口、持续时间和取消操作。
- 新增任务卡片，展示服务器 IP、端口、持续时间、检查次数及最近一次结果。
- 新增 `checktest.py`，每 5 秒执行一次 TCP 连接检查，单任务最长 320 秒。
- 管理后台新增授权、积分、卡槽配置和已授权账号统计。

### 修复

- 点击“取消任务”后立即移除对应任务方框，并从后端任务列表中删除，刷新页面后不会再次出现。
- 将联通性检查的前端、后端和 Python 脚本最长持续时间统一限制为 320 秒。

### 安全限制

- 任务参数通过参数数组传递给 Python，不启用 Shell，避免命令注入。
- IP、端口和持续时间均在服务端校验。
- 单账号并发任务数受卡槽限制，卡槽最多 10 个。
- 联通性检查只建立普通 TCP 连接，不发送压力流量。
